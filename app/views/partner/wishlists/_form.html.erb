<%= form_for [:partner, @wishlist.campaign.organization, @wishlist.campaign, @wishlist], :html => {:class => "form-horizontal"} do |f| %>
  <%= render partial: 'common/wishlists/form_fields', locals: {f: f, back_url: partner_organization_campaign_url(@wishlist.campaign.organization, @wishlist.campaign)} %>
<% end %>
