<div class="panel panel-default outer-panel panel-info">
  <!-- Default panel contents -->
  <div class="panel-heading"><h3 class="panel-title">Survey Management</h3></div>
  <div class="panel-body">
  </div>
  <div class="panel panel-default outer-panel">
    <!-- Default panel contents -->
    <div class="panel-heading"><h3 class="panel-title">Surveys</h3></div>
      <table class="table table-striped table-condensed">
        <caption><%= link_to '<i class="glyphicon glyphicon-plus"></i> New Survey'.html_safe, new_admin_survey_url, :class => "btn btn-xs btn-primary" %></caption>
        <thead>
          <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Disabled?</th>
            <th>&nbsp;</th>
          </tr>
        </thead>
        <tbody>
      <%= render :partial => 'survey', :collection => @surveys %>
        </tbody>
      </table>
    </div>
</div>
