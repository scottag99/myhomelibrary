<div class="panel panel-default outer-panel">
  <!-- Default panel contents -->
  <div class="panel-heading"><h3 class="panel-title">Catalog Management</h3></div>
  <div class="panel-body">
    <p>The following catalogs are loaded into the system. Only books from <strong>active</strong> catalogs are displayed in search results.</p>
  </div>
  <div class="panel panel-default outer-panel">
    <!-- Default panel contents -->
    <div class="panel-heading"><h3 class="panel-title">Catalogs</h3></div>
      <table class="table table-striped table-condensed">
        <caption><%= link_to '<i class="glyphicon glyphicon-plus"></i> New Catalog'.html_safe, new_admin_catalog_url, :class => "btn btn-xs btn-primary" %></caption>
        <thead>
          <tr>
            <th>Name</th>
            <th>Active?</th>
            <th>Source</th>
            <th>Book Count</th>
            <th>&nbsp;</th>
          </tr>
        </thead>
        <tbody>
      <%= render :partial => 'catalog', :collection => @catalogs %>
        </tbody>
      </table>
    </div>
</div>
