<%= form_for [:admin, @wishlist.campaign.organization, @wishlist.campaign, @wishlist], :html => {:class => "form-horizontal"} do |f| %>
  <%= render partial: 'common/wishlists/form_fields', locals: {f: f, back_url: admin_organization_campaign_url(@wishlist.campaign.organization, @wishlist.campaign) } %>
<% end %>
