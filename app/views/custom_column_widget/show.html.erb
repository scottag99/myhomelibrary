<div class="row <%= widget.classname %>">
  <% widget.columns.each do |column| %>
    <div class="col-md-<%= column.width %>">
      <%= scrivito_tag(:div, widget, column.widget_list_attr_name) %>
    </div>
  <% end %>
</div>
